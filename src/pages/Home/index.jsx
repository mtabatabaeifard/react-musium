import { Button } from '@mui/material';
import React from 'react';

function Home() {
    return (
        <div>
            Home
            <Button variant="contained" color="primary">
                test
            </Button>
        </div>
    );
}

export default Home;
